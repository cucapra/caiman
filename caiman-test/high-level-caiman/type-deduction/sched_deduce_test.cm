#version 0.1.0

tmln time(e: Event) -> Event { returns e }
sptl space(bs: BufferSpace) -> BufferSpace { returns bs }

val main() -> i32 {
    a :- 2
    c :- 10
    b :- a * c
    d :- b + a
    e :- d - c
    t :- 3
    k :- e / t
    returns k
}

fn main_impl() -> i32 @ node(main.k)-usable
    impls main, time, space
{
    let a @ node(main.a) = 2;
    let c @ node(main.c) = 10;
    let b @ node(main.b) = a * c;
    let d @ node(main.d) = b + a;
    let e @ node(main.e) = d - c;
    let t @ node(main.t) = 3;
    let k @ node(main.k) = e / t;
    k
    
}

pipeline main { main_impl }