// Just for AST

let x = 1;
// Comment 
let mut y = 42.06; // 9

let x2 = x;

let j = x2 + 1;
let three_plusses = 1 + 2 + 3;
let asdf = (j + 1) + 2 + (3 + 4);

let minus = 1.9 - 2.1;

let mult = 2 * 3;
let mult2 = 2 * 3 + 1;
let mult3 = 2 * (3 + 1);

let div = 4 / 3;
let div2 = 4 / 3 * 2;
let div3 = 1 + 5555 / 2 + 3 * 3;

let bool1 = true && false || false && true;
let weirdmix = 1 * 3 && false + 50 == true;
let weirdmix2 = 1 == 2 || true == (false && 8) / 3 * 1;

let in = input();
let b = true;

let True = false;

let actually_false = !true;
let negativeone = -1;
let binops_unops = !5 + -3 / 2 - -3;
let at_end = 1 + !5;

let call = f(1 + 1, 2 + 2, g, g / 6);
let unit_call = procedure();

let labeledexp = { l1: 5 };
let labeledexp_bop = g + { l2: 9 + 9 + 9 * 9999099098 };
let labelrecursion = { l: { l2: { l3: 5 } } };

let adding_to_fcall = f() + 1 + g(1000, h(1, 2, 3));
