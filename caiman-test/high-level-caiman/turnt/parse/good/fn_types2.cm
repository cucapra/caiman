#version 0.1.0
type gint = i32'<storage>;

fn foo(a: gint'<map_write> @ input(v.x)-?, 
       b: bool @ [],
       c: i64'<map_read, alignment_bits = 512> @ [node(v.y)-dead, input(tm.z)-save, output(sp.k)-need]) 
       -> i32'<storage, map_write>
        impls v, tm, sp
{
    
}