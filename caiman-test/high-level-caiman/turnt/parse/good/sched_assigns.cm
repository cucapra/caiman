#version 0.1.0
fn foo(a: i32, b: bool'<storage> @ [?(v)-?, ?(sp)-usable])
    -> (i32'<storage> @ [output(n)-?, none(v)-?, none(sp)-usable], i32)
    impls n, v, sp
{
    var x = a + a * a;
    x = b;
}