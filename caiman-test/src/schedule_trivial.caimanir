version 0.0.1

types [
    i32,
    slot $slot0 {
        type : i32,
        stage : ready,
        place : local,
    },
    event $event0 {
        place : local
    },
]

external_cpu i32 @do_thing_on_cpu(i32);

value i32 @foo(i32) {
    %0 = phi 0;
    %1 = constant 3i32;
    return %1;
}

extras {
}

pipeline "pipeline_schedule_trivial" = @foo;