let main(x : slot) : main -> slot {
    return $result.sum_sch(?);let
}

let sum_sch(x : slot) : sum -> slot {
    $result.if(?) {
        allocate $left_res;
        
        $left.?;
        $left_res.?
    }
    else { ??? }
    return ?;
}