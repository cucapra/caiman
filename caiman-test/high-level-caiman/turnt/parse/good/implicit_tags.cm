#version 0.1.0

fn foo(a: i64 @ main-usable, b: i64 @ main.x-usable, c: i32) -> (i32 @ output(val.y)-usable,) 
    impls main 
{
    let x @ main-usable = 10;
    let y @ node(main)-usable = 10;
    let z @ node(val.x) = 20;
    let w: i64 @ main.y-usable = 30;
}