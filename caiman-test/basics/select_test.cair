version 0.0.2

type i64;
ref %i64l : i64-local<flags=[]>;
event %event0;
buffer_space %buffspace;
native_value %i64n : i64;

function @main() -> i64;

value[impl default @main] %foo() -> i64 {
    %x = constant %i64n 0;
    %y = constant %i64n 1;
    %z = constant %i64n 2;
    %r = select %x %y %z;
    return %r;
}

timeline %time(%e : %event0) -> %event0 {
    return %e;
}

spatial %space(%bs : %buffspace) -> %buffspace {
    return %bs;
}

schedule[value $val = %foo, timeline $time = %time, spatial $space = %space]
%foo_main<none($time.%e)-have, none($time.%e)-have>() ->
[%out : node($val.%r)-have none($space.%e)-have none($time.%e)-have %i64n] {
    %x_loc = alloc-temporary local [] i64;
    local-do-builtin node($val.%x)() -> %x_loc;
    %x_val = read-ref i64 %x_loc;

    %r_loc = alloc-temporary local [] i64;

    %djoin = default-join;
    %join = inline-join %foo_ret [] %djoin;

    schedule-select %x_val [%foo_left, %foo_right]
        [value node($val.%r), timeline none($time.%none), spatial none($space.%none)]
        (%r_loc) %join;
}

schedule[value $val = %foo, timeline $time = %time, spatial $space = %space]
%foo_left<none($time.%e)-have, none($time.%e)-have>(%r_loc : none($val.%e)-none none($space.%e)-met none($time.%e)-have %i64l) ->
[%out : node($val.%y)-have none($space.%e)-met none($time.%e)-have %i64l] {
    local-do-builtin node($val.%y)() -> %r_loc;
    return %r_loc;
}

schedule[value $val = %foo, timeline $time = %time, spatial $space = %space]
%foo_right<none($time.%e)-have, none($time.%e)-have>(%r_loc : none($val.%e)-none none($space.%e)-met none($time.%e)-have %i64l) ->
[%out : node($val.%z)-have none($space.%e)-met none($time.%e)-have %i64l] {
    local-do-builtin node($val.%z)() -> %r_loc;
    return %r_loc;
}

schedule[value $val = %foo, timeline $time = %time, spatial $space = %space]
%foo_ret<none($time.%e)-have, none($time.%e)-have>
(%r_loc : node($val.%r)-have none($space.%e)-met none($time.%e)-have %i64l) -> [%out : node($val.%r)-have none($space.%e)-have none($time.%e)-have %i64n] {
    %r_val = read-ref i64 %r_loc;
    return %r_val;
}

pipeline "main" = %foo_main;