version 0.0.1

types [
    i64,
    slot $slot0 {
        type : i64,
        stage : ready,
        place : local,
    },
    event $event0 {
        place : local
    },
]

external_cpu i64 @do_thing_on_cpu(i64);

value i64 @foo(%0 : i64) {
    %1 = constant 4i64;
    return %1;
}

extras {}

pipeline "pipeline_trivial" = @foo;